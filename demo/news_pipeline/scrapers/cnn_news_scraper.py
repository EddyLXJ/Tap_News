import requests

USER_AGENTS_FILE = os.path.join(os.path.dirname(__file__))
with open(USER_AGENTS_FILE, 'r') as uaf:
    for ua in uaf.readlines():

def getHeader()

def extract_news(new_url):
    # Fetch html
    session_requests = requests.session()
    # Parse html

    # Extract_information
    return None
